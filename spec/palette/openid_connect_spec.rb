require 'spec_helper'

describe Palette::OpenidConnect do
  it 'has a version number' do
    expect(Palette::OpenidConnect::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
